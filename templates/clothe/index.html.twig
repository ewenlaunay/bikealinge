{% extends 'base.html.twig' %}

{% block title %}Clothe index{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center d-inline-block">
        <h1>Contenu de la commande</h1>
    </div>
    <form action="{{ path('order_index') }}">
        <div class="container d-flex justify-content-between card-body jumbotron jumbotron-fluid col-md-12">
            <div class="col-md-6">
                <h2>Adultes</h2>
                {% for clothe in clothes %}
                    <ul>
                        <li style="list-style-type: none">{{ clothe.title }}</li>
                        <button type="button" class="btn btn-primary">-</button>
                        <button type="button" class="btn btn-primary">+</button>
                        <i class="fab fa-facebook"></i>
                    </ul>
                {% endfor %}
            </div>
            <div class="col-md-6">
                <h2>Enfants</h2>
                {% for clothe in clothes %}
                    <ul>
                        <li style="list-style-type: none">{{ clothe.title }}</li>
                        <button type="button" class="btn btn-primary">-</button>
                        <button type="button" class="btn btn-primary">+</button>
                    </ul>
                {% endfor %}
            </div>
        </div>
        <br>
        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-primary">Suivant</button>
        </div>
    </form>







    {#<h1>Clothe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Composition</th>
                <th>WeightAdult</th>
                <th>WeightChild</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for clothe in clothes %}
            <tr>
                <td>{{ clothe.id }}</td>
                <td>{{ clothe.title }}</td>
                <td>{{ clothe.composition }}</td>
                <td>{{ clothe.weightAdult }}</td>
                <td>{{ clothe.weightChild }}</td>
                <td>
                    <a href="{{ path('clothe_show', {'id': clothe.id}) }}">show</a>
                    <a href="{{ path('clothe_edit', {'id': clothe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('clothe_new') }}">Create new</a>#}
{% endblock %}
